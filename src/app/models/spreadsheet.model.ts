// src/app/models/parsed-row.model.ts
export interface ParsedRow {
    [key: string]: string; // Use dynamic keys if necessary
    // Or explicitly define known keys:
    // name?: string;
    // winrate?: string;
    // deck?: string;
  }
  